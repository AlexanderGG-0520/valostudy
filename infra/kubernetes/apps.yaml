apiVersion: v1
kind: ConfigMap
metadata:
  name: valostudy-config
data:
  BETTER_AUTH_URL: https://valostudy.alec-ofc.com
  S3_ENDPOINT: https://f74f93d265d7e4bd911aa44934795ab6.r2.cloudflarestorage.com
  S3_REGION: auto
  S3_BUCKET: valostudy
  STRIPE_PLUS_PAYMENT_LINK_URL: https://buy.stripe.com/5kQbJ0gR2fp9alR1ow9IQ04
  STRIPE_PRO_PAYMENT_LINK_URL: https://buy.stripe.com/cNi6oG58k1yj3Xtd7e9IQ05
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: valostudy-web
spec:
  replicas: 2
  selector:
    matchLabels: { app: valostudy-web }
  template:
    metadata:
      labels: { app: valostudy-web }
    spec:
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: web
          image: ghcr.io/alexandergg-0520/valostudy-web:ee069f0120a994d34160591e84e944c9e3ce829a
          ports: [{ containerPort: 3000 }]
          envFrom:
            - configMapRef: { name: valostudy-config }
            - secretRef: { name: valostudy-secrets }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: "1", memory: 1Gi }
          securityContext: { allowPrivilegeEscalation: false, capabilities: { drop: ["ALL"] } }
          readinessProbe:
            httpGet: { path: /api/health, port: 3000 }
          livenessProbe:
            httpGet: { path: /api/health, port: 3000 }
            initialDelaySeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: valostudy-web
spec:
  selector: { app: valostudy-web }
  ports: [{ port: 80, targetPort: 3000 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: valostudy-worker
spec:
  replicas: 1
  selector:
    matchLabels: { app: valostudy-worker }
  template:
    metadata:
      labels: { app: valostudy-worker }
    spec:
      terminationGracePeriodSeconds: 660
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: worker
          image: ghcr.io/alexandergg-0520/valostudy-worker:ee069f0120a994d34160591e84e944c9e3ce829a
          envFrom:
            - configMapRef: { name: valostudy-config }
            - secretRef: { name: valostudy-secrets }
          resources:
            requests: { cpu: "1", memory: 512Mi, ephemeral-storage: 5Gi }
            limits: { cpu: "2", memory: 2Gi, ephemeral-storage: 8Gi }
          securityContext: { allowPrivilegeEscalation: false, capabilities: { drop: ["ALL"] } }
          volumeMounts: [{ name: scratch, mountPath: /tmp }]
      volumes:
        - name: scratch
          emptyDir: { sizeLimit: 6Gi }
